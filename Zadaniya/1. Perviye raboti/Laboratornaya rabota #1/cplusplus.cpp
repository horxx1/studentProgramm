#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");

    double x;
    double y;

    cout << "Введите первое число: ";
    cin >> x;
    cout << endl;

    cout << "Введите второе число: ";
    cin >> y;
    cout << endl;

    double numInExp = x / (2 * y)
    double numInSqrt = 1

    double d = 10^4 * ((exp(numInExp) + sqrt(num))/(2,5 * cos^2 * x))
}